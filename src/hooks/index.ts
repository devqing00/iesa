/**
 * Custom Hooks
 * 
 * Shared hooks for the IESA platform
 */

export {
  useQuery,
  useMutation,
  formatApiError,
  clearQueryCache,
  type QueryResult,
  type MutationResult,
} from './useQuery';
